<script lang="ts">
	import type { PageData } from './$types';

	export let data: PageData;
</script>

<h2>Keypoints</h2>

<ol>
	<li>`v` from `vh` & `vw` is viewport; `h` is height, `w` is width</li>
	<li>value before that is percentage of viewport unit</li>
	<li>`vmin` & `vmax` for limit the minimal & maximal of viewport value</li>
</ol>
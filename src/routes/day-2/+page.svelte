<script lang="ts">
	import type { PageData } from './$types';

	export let data: PageData;
</script>

<h2>Keypoints</h2>
<ol>
	<li>use `rem` to fontsize</li>
	<li>avoid using `em` on list markup (prevent multiplication on nested element)</li>
	<li>`em` is relates to current font size element</li>
</ol>
